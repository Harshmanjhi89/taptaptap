services:
  - type: web
    name: your-project-name
    env: bun
    buildCommand: "bun install"  # Install dependencies with Bun
    startCommand: "bun run start"  # Adjust if your start command is different
